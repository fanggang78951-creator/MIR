# 攻速突破平台沉淀实施计划

## 1. 基线与备份

- 记录E盘平台关键文件、两张装备表和D盘金样本哈希。
- 检查武器附魔残留，不删除其他任务的 `_codex_tmp` 或事务证据。
- 修改既有文件前建立本轮平台备份并回读哈希。

## 2. RED测试

- 非常驻包资源、三挂钩、TextVar三值与测试命令隔离。
- 0、20、30可用，31明确阻止。
- 多件固定装备逐项累加、总和封顶30、唯一 `ChangeSpeed 2`。
- 未安装包时做装备预检阻止。
- 预检零写入、重复安装幂等、安装/回滚逐字节恢复。
- 既有脚本属性和武器附魔专项回归不变。

## 3. 最小实现

- 新增 `attack_speed_breakthrough.py` 专项服务和43号中文配置表解析。
- 接入配置同步路由、CLI回滚选项和候选包注册。
- 扩展做装备TextVar显示写入与包依赖门禁。
- 在脚本属性注册表中增加“攻速突破”。

## 4. 工作簿

- 用artifact-tool更新正式09号共享母表和 `做装备/templates/XuanYuanItems.xlsx`。
- 新增43号包配置表并登记到填写文档注册表。
- 保持既有样式、下拉、说明和列顺序；逐页渲染检查。

## 5. 验证与交付

- 跑攻速突破专项、做装备专项、配置同步与必要回归。
- 正式构建GUI/CLI并核验冻结源码。
- 用冻结CLI对 `D:/MirServer` 只读预检，前后哈希一致。
- 更新接口、接口清单、维护入口与交班记录，状态保持跨服待验收。

