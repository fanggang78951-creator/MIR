# 完整武器衣服动作素材实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 104 套武器和 55 套衣服 PNG 动作素材以可回滚方式成为做装备可选的备用 `Looks/Shape` 资源。

**Architecture:** 新增独立动作素材编译器，读取动作目录和 Placement 坐标，构建客户端副本中的 WZL/WZX；由同一清单生成静态三图库、资源映射及退役清单。正式客户端仅接收通过回读验证的成对文件，服务端零写入。

**Tech Stack:** Python 3、Pillow、`wzl_blackbox.transaction`、`wzl_blackbox.inspector`、unittest、CSV/JSON、SHA256。

**Spec:** `E:\XuanYuanDevPlatform\docs\specs\2026-09-10-完整武器衣服动作素材设计.md`

## Global Constraints

- 仅使用翎风/LFM2；不启动、关闭或操作游戏引擎。
- 不写服务端和数据库；不删除旧帧或旧映射。
- 静态新增编号固定跳过 `7098–7111`，使用连续 `7112–7325`。
- 每个动作 Shape 固定 1200 槽；仅有效 PNG 写 type6，空 PNG 保留空槽。
- 正式文件必须先在 workspace 副本成功回读，并以整组备份/回滚方式部署。

---

### Task 1: 动作清单与静态图规范

**Files:**
- Create: `E:\XuanYuanDevPlatform\做装备\src\xyequip\resources\action_resource_bundle.py`
- Create: `E:\XuanYuanDevPlatform\做装备\tests\test_action_resource_bundle.py`

**Interfaces:**
- Consumes: `build_action_manifest(source_root, static_start=7112)`。
- Produces: `ActionManifest`，含 104 武器、52 衣服、208 静态资源和退役 Looks 集合。

- [ ] 先写失败测试：断言源编号区间、104/55 目录、静态 `7112–7325` 和 1200 槽规则。
- [ ] 运行该测试，确认因模块不存在而失败。
- [ ] 实现只读清单生成：按数字排序目录，保留原目录名、每帧 PNG、Placement 坐标和源 SHA256。
- [ ] 运行专项测试，确认通过。

### Task 2: WZL/WZX 副本构建与回读

**Files:**
- Modify: `E:\XuanYuanDevPlatform\wzl编辑\src\wzl_blackbox\transaction.py`
- Modify: `E:\XuanYuanDevPlatform\wzl编辑\tests\test_action_sequence_transaction.py`
- Modify: `E:\XuanYuanDevPlatform\做装备\src\xyequip\resources\action_resource_bundle.py`
- Modify: `E:\XuanYuanDevPlatform\做装备\tests\test_action_resource_bundle.py`

**Interfaces:**
- Consumes: `compile_action_manifest(manifest, staging_data, backup_root)`。
- Produces: 每个动作库的 before/after SHA256、Shape 起点、1200 帧回读结果。

- [ ] 先写失败测试：使用临时 WZL/WZX 与一个含空槽/负坐标的 1200 帧夹具，断言 `append_png_sequence_transaction` 一次提交有效帧 type6、空帧 offset=0、坐标未变。
- [ ] 运行该测试，确认因编译函数不存在而失败。
- [ ] 在编码器实现公开 `append_png_sequence_transaction(wzl_path, wzx_path, frames, backup_root)`；它只调用既有 type6 编码、成对构建和 `_commit_pair`，不改变未修改帧。
- [ ] 使用该公共事务 API 写入副本；禁止装备平台直接调用编码器私有函数或重排 WZL。
- [ ] 运行专项测试及编码器 unittest，确认通过。

### Task 3: 静态三图库与映射退役

**Files:**
- Modify: `E:\XuanYuanDevPlatform\做装备\src\xyequip\resources\action_resource_bundle.py`
- Modify: `E:\XuanYuanDevPlatform\做装备\tests\test_action_resource_bundle.py`
- Modify: `E:\XuanYuanDevPlatform\做装备\profiles\装备资源映射表.csv`

**Interfaces:**
- Consumes: `render_static_icons(manifest, output_root)`、`apply_mapping_update(map_csv, manifest, dry_run=True)`。
- Produces: 208 个同号三图库帧、208 条 candidate 映射、60 条已退役武器/男女衣服映射。

- [ ] 先写失败测试：断言三图库同号、`7098–7111` 为空槽、旧 60 条退役且其他映射逐字节不变。
- [ ] 运行该测试，确认因函数不存在而失败。
- [ ] 实现动作展示图到静态三图的透明裁切/画布适配，并以完整临时 CSV 进行映射更新验证。
- [ ] 运行专项测试，确认通过。

### Task 4: 预检、部署与回滚收据

**Files:**
- Modify: `E:\XuanYuanDevPlatform\做装备\src\xyequip\resources\action_resource_bundle.py`
- Create: `E:\XuanYuanDevPlatform\做装备\tools\import-complete-weapon-clothing-assets.py`
- Modify: `E:\XuanYuanDevPlatform\做装备\tests\test_action_resource_bundle.py`

**Interfaces:**
- Consumes: `preflight_bundle(...)`、`apply_bundle(...)`、`rollback_bundle(receipt)`。
- Produces: JSON 收据，包含所有输入/输出/备份哈希、Shape/Looks 映射、回滚结果。

- [ ] 先写失败测试：模拟副本写入失败，断言全部 WZL/WZX 与 CSV 恢复到 before SHA256。
- [ ] 运行该测试，确认因部署函数不存在而失败。
- [ ] 实现预检、同卷临时文件原子替换、哈希确认与失败回滚；正式目标只允许由 `--apply` 明确触发。
- [ ] 运行专项测试、编码器完整 unittest、真实副本预检；记录用户游戏验收项。

### Task 5: 候选导入与交班

**Files:**
- Create: `E:\XuanYuanDevPlatform\做装备\assets\complete-weapon-clothing-source\manifest.json`
- Create: `E:\XuanYuanDevPlatform\做装备\assets\complete-weapon-clothing-source\README.md`
- Modify: `D:\codex交班记录\00_任务总结索引.txt`
- Modify: `D:\codex交班记录\01_最近任务指针.txt`
- Create: `D:\codex交班记录\20260910_完整武器衣服动作素材导入.txt`

- [ ] 复制前记录正式目标哈希，创建同卷备份并在副本执行导入。
- [ ] 对副本执行完整回读；仅成功后部署目标客户端与已确认的登录器 Data 源。
- [ ] 回读正式文件、收据和平台映射；清理可删除临时施工输出，保留事务备份与证据。
- [ ] 写交班，明确标注客户端游戏验收为待验证。
