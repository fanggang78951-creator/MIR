# 武器附魔 V2 实施计划

> 本计划遵循TDD。生产代码改动前必须先看到对应测试因缺少V2能力而失败。

## 1. 基线与备份

- 记录活动平台、verified装备洗练母版、武器附魔源码、测试、42号表、接口和成果包哈希。
- 复制关键文件到 `backups/20260830_110707_weapon_enchant_v2_before` 并回读哈希。
- 跑 `tests.test_equipment_wash_opening_package` 与 `tests.test_weapon_enchant` 基线。

## 2. RED：新增失败测试

- 新42号V2五表可解析；旧五表继续兼容。
- 普通洗练母版保持原哈希且派生脚本包含其业务标签。
- 六条技能词条与任意新增候选使用稳定编码和唯一ID。
- 整数权重编译为精确RANDOMEX剩余池链，每个命中分支立即GOTO并BREAK。
- 默认100元宝可改，且余额检查发生在扣费前。
- 仅武器StdMode 5、6；普通母版包不被修改。
- 路由为candidate或缺少宿主时预检阻止；无纯文字伪实效。
- 单Lock/Update连续写入、GB18030/CRLF、无SQLite/ApexM2.DB。
- 当前服冻结预检不产生写入。

## 3. GREEN：最小实现

- 重构 `src/xydp/weapon_enchant.py`，增加V2数据模型、旧表兼容和verified母版派生器。
- 将技能身份与普通/脚本属性路由编译为独立受管块。
- 保持配置同步route、GUI控件和按钮位置不变。
- 用artifact-tool重做42号中文工作簿并保留备份。

## 4. 文档与注册

- 更新武器附魔candidate包 manifest/README。
- 更新接口46和接口清单/维护记录中当前版本信息。
- 不修改verified装备洗练包。

## 5. 验证与构建

- 跑武器附魔专项、装备洗练回归和相关配置同步测试。
- 跑平台全量测试；若已有基线失败，记录首个独立失败并确认与本改动关系。
- 渲染42号表全部工作表，检查布局、公式和错误值。
- 正式构建GUI/CLI，记录SHA256。
- 使用冻结CLI对 `D:/MirServer` 做只读预检；不安装。

## 6. 收尾

- 清理开发临时文件，不删除正式事务证据。
- 生成独立交班并更新共享索引/最近指针。
- 状态区分“平台静态完成”和“组合路线游戏待验证”。

