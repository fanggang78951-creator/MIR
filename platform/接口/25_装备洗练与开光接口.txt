装备洗练与开光接口：1.2.0-candidate.3，游戏待验证
唯一任务：20260907_1541_平台总控与可靠部署

本版修复外部同级GOTO未导入问题，随机核心全部显式CALL且无递归环；各预期槽完整验证后才扣100元宝和覆盖属性。洗练同步执行，不再排延迟回调。概率、TXT数值、品质和既有开光收费规则保留。普通等权池排除当前攻魔道上限不足2的类别，等价于原拒绝重抽。
原生0/21及TextVar40、43～55允许映射分别验证，未开光稀有品质可合法零词条。
新GUI/CLI/源码/包必须匹配catalog/current-release.json；candidate不能继承旧版游戏验收。安装、文件恢复与实际游戏效果分开记录。
重复开光入口尚未完成消耗品失败无损改造；不得把洗练提交前无扣费/无物品写入结论扩大到开光或引擎中途写入失败。
临时无消耗诊断只按接口50原事务恢复，不得裸通用回滚丢失安装登记。正式新版安装状态以最新总控交班为准。

唯一执行入口：GUI“项目总览”选择洗练 → “转到预检入口” → “脚本配置同步”。
CLI必须从catalog/current-release.json解析cli.path并核对cli.sha256；禁止猜测EXE文件名，发布记录与候选文档不一致时停止执行。

预检：
  <匹配CLI> --root <平台目录> config-sync-preflight --server <服务端目录> --client <客户端目录> --file <洗练属性.txt>
总控确认具体差异后安装：
  <匹配CLI> --root <平台目录> config-sync-install --server <服务端目录> --client <客户端目录> --file <洗练属性.txt> --yes
按原事务回滚：
  <匹配CLI> --root <平台目录> config-sync-rollback --server <服务端目录> --transaction <事务编号> --route equipment-wash-import --yes

目标要求：翎风/LFM2；真实地图、唯一NPC、已声明的战斗/处决/续航/攻速依赖；TextVar40及43～55兼容；客户端已有匹配XY_EquipmentWorkbench资源。缺项时零写入，不自动补数据库、客户端资源或其他实验室能力。

测试数量、隔离事务、目标写入和回滚结果只从对应版本验收报告与事务收据读取，不在接口固定。任何自动测试证据都不能替代M2或游戏验收。
验收必须使用对应发布生成的新洗练装备，覆盖普通至神佑各条数、零有效结果、原生0/21正值、成功提示时点、连续点击、忙中点击、旧CD遗留、27/28各自解锁、换出换入、首次/重复开光、保存重登、悬浮显示、穿脱实效及费用。通过前状态只能写candidate/待验证。
