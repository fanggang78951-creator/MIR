# 表格检查说明

- 两份 XLSX 均由 `@oai/artifact-tool` 2.8.6 创建并重新导入检查。
- 工作表、表格和公式检查结果保存在同目录的 NDJSON/JSON 文件中。
- 公式错误扫描：`#REF!`、`#DIV/0!`、`#VALUE!`、`#NAME?`、`#N/A`、`#NUM!` 均为 0。
- 本机 `artifact-tool workbook.render()` 在最小测试工作簿上也会触发 Windows 原生进程崩溃（退出码 `-1073741819`），并非本工作簿内容错误。
- 为完成视觉核验，使用本机 Excel 12.0 以只读方式导出 PDF，再转为 PNG 检查。已确认标题、表头、颜色、换行、状态汇总和数据区域可见，无重叠或公式错误。
- Excel/PDF/PNG 检查过程均未修改正式服务端、平台、M2或游戏引擎。
