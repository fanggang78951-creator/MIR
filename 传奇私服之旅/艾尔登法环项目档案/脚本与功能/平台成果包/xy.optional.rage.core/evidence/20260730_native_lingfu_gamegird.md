# 原生灵符 GAMEGIRD 静态证据

- 狂暴开启费用统一使用 `CHECKGAMEGIRD > (费用-1)` 与 `GAMEGIRD - 费用`。
- 对话余额统一读取 `<$GAMEGIRD>`。
- `灵符`、`账户灵符`、`原生灵符`在初始之地材料表中均映射为同一原生货币，不再生成 `CHECKITEM 灵符`、`TAKE 灵符`、`CHECKGAMEDIAMOND` 或 `GAMEDIAMOND -`。
- 击杀者奖励仍是原有的账户金刚石奖励，它不是灵符费用，未改变业务含义。
- 当前仍需游戏内验证余额不足、刚好足够、重复开启和图标状态。
