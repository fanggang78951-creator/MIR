# 狂暴中文编码与旧命令块安全接管

- 包版本：`xy.optional.rage.core 2.1.1-candidate.1`
- 发现现象：脚本文件存在，但图标调用不能激活，状态变量不更新。
- 根因：UTF-8母稿被原样复制到目标服；M2按GB18030读取后，中文配置路径、键名和费用类型变为乱码，功能开关读取失败。
- 修复：核心脚本使用 `transcode_copy`，从UTF-8母稿转为GB18030/CRLF后写入目标服。
- 升级兼容：managed_block声明 `legacy_package_ids=["xy.optional.rage.command"]`。只有旧块正文哈希与标记一致且新旧块不并存时才原位接管；手改或并存必须阻止。
- 测试床事务：`20260725_195642_9bef938e`。
- 测试床结果：`XY_RAGE_COMMAND`唯一1处；旧marker消失、新marker唯一；GB18030解码能读取 `玄渊配置\狂暴配置.txt` 和 `账户金刚石`；再次预检0变化；逐字节回滚通过。
- 自动测试：平台全量 `215/215` 通过。
- 正式服事务：`20260725_201943_8f1ffded`；安装后命令标签唯一、旧marker为0、新marker唯一，GB18030中文静态检查通过，再次预检0变化。
- 正式服游戏验收：待用户验证费用确认、激活、彩色图标、死亡/下线清理。
