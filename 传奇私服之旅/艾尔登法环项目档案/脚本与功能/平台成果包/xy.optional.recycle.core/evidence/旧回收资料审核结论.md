# 旧回收资料审核结论

## 可复用结论

1. `XY-SKILL-008` 证明背包自定义按钮统一走 `CustomButtonClick`，历史按钮ID 20只负责路由，不承载业务扣除。
2. `XY-SKILL-021` 证明QFunction内联页面、UserCmd31/32/33和24栏分页框架曾被验证。
3. 旧服 `UserCmd31` 留下了实际可执行的最小白名单：12项低阶装备，每项最多10件，每件1000金币。
4. 当前 `D:\MirServer\Mud2\DB\StdItems.DB` 原始字节能找到12个装备名与“金币”。

## 不发布内容

1. 24栏页面中的栏位名称、分类、一键测试和自动开关均为占位，真实规则未接入。
2. 外部 `#CALL` 路线在当前服已经由M2日志实证无法加载，本包把页面、确认和全部业务规则完整放进QFunction受管块。
3. 明月端 `TakeBagItem` 脚本依赖明月装备名、货币、标志位和1744至1749界面资源，不植入当前服。
4. 不接自动回收，不使用模糊关键词，不按部位或等级猜测可回收物。

## 当前候选策略

- 玩家命令 `@快捷回收` 映射 `UserCmd31`。
- QFunction页面先列明范围并二次确认。
- 实际12项规则与120组扣除奖励完整内联在QFunction同一受管块，不创建独立核心脚本。
- 包状态保持 `candidate/optional`，当前服游戏验收通过后再晋级。
