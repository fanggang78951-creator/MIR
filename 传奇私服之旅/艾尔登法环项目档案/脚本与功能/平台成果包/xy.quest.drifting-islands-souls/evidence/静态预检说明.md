# 静态预检说明

- 三张地图已在当前目标服 `MapInfo.txt` 注册，对应 MAP 文件存在。
- `XX113(116,104)`、`XX329(78,55)`、`XX104(85,23)` 均由平台 MAP 解析器判定为可走坐标。
- 三个坐标未被当前 `MerChant.txt` 中其他 NPC 占用。
- 13种任务材料在当前 `ApexM2.DB/StdItems` 中均唯一存在。
- 三个称号名称未被占用，`StdMode=70` 的 Shape 40、41、42 未被占用。
- NPC 外观 220、222、226 已在当前客户端和现有 NPC 中验证可用，本包只复用，不覆盖 NPC 补丁。
- 韧性统一接入现有 `XY_EXECUTION_LAB_TOUGHNESS_ANCHOR`，不建立第二套处决/韧性系统。
- 游戏内验收与逐字节回滚仍为必需项。
