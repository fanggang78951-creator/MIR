# 禁止作为当前属性读取参考：40/41/46/47/48直读已验证失效

> 本文只保留历史过程。狂暴桥部分仍有历史价值，但属性A/B默认直读40/41/46/47/48会显示0，禁止重新采用。当前唯一正确读取链见`20260726_final_attribute_read_contract.md`。

# 装备显示直读与狂暴按钮桥修复（历史原文）

- 版本：`xy.ui.attr-overview 2.0.3-candidate.1`
- 属性面板默认使用做装备已写入的显示属性位：40、41、42、46、47、48、49、50、51、52、53。
- 每项同时汇总`GetAllCustomItemValue`的点数与百分比值；只用于界面显示，不进入神力、怪伤、爆伤等业务实效链。
- `TakeOnEx/TakeOffEx`直接调用界面刷新，不再新增`DELAYGOTO`，避免干扰登录及换装重算。
- 狂暴静态按钮ID 3跳转到QFunction内确认面板，确认后只调用`@XY_RAGE_TRIGGER`，并立即重画狂暴图标。
- 旧按钮88在界面刷新时清理，避免与世界地图静态按钮入口重叠。
- 自动测试必须覆盖属性位直读、任意变量回退模式、脚本注入拒绝、狂暴桥和回滚。
