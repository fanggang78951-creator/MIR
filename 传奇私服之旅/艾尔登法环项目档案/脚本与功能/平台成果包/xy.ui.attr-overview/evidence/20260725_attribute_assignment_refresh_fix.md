# 禁止作为当前刷新时序参考：1秒/2秒桥已被5拍补绘取代

> 本文只保留历史过程。当前登录和换装均使用立即刷新加`DELAYGOTO 5 @XY_UI_STATUS_REFRESH`，不得恢复旧1秒/2秒桥。当前唯一正确逻辑见`20260726_final_attribute_read_contract.md`。

# 属性逐项赋值与换装快速刷新修复（历史原文）

- 包版本：`xy.ui.attr-overview 2.0.2-candidate.1`
- 发现现象：每个面板只有第一项能显示真实值，后续项目保持0。
- 根因：上一项的 `SMALL ... 0` 条件段之后直接输出下一项 `MOV`，导致下一项赋值落在上一条件的 `#ACT` 内。
- 修复：每个属性赋值前显式生成独立 `#IF/#ACT` 无条件段；各项负数保护仍只作用于本项。
- 换装刷新：`TakeOnEx/TakeOffEx` 第1秒调用 `@XY_UI_QF_EQUIP_REFRESH`，第2秒调用 `@XY_UI_QF_STATUS_REFRESH`，兼顾快速显示和延迟2秒的爆伤最终汇总。
- 测试床事务：`20260725_195642_9bef938e`。
- 测试床结果：联合安装后再次预检0变化；12个source全部存在；第1秒/第2秒桥接均存在；逐字节回滚后3个原始文件SHA-256完全恢复。
- 自动测试：平台全量 `215/215` 通过。
- 正式服事务：`20260725_201943_8f1ffded`；安装后再次预检0变化，目标哈希与收据一致。
- 正式服游戏验收：待用户在M2重载后验证换装1秒刷新及2秒最终补齐。
