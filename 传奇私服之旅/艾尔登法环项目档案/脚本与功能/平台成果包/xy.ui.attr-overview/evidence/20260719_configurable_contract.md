# 可配置属性绑定自动验收证据

- 版本：`1.2.0-candidate.1`
- 日期：2026-07-19
- 状态：自动化与隔离事务通过，独立新服游戏验收待执行。

## 已验证

1. `attr_panel`可将默认三项整组替换为“暴击伤害 + 固定切割”。
2. 生成脚本读取`N$XY_最终爆伤`和`N$XY_固定切割`，不残留默认怪伤、神力行。
3. 名称、顺序、偏移、前后缀、颜色和最小值均按JSON编译。
4. 空面板、超过12项、非法来源变量、反斜杠和脚本保留字符会阻止预检。
5. 源码专项11项、平台160项、装备74项通过。
6. 正式冻结CLI完成隔离安装、重复预检零变化和逐字节回滚。

## 尚未宣称

本证据不代替M2启动与游戏内悬浮验收。至少两种非默认属性组合在独立同引擎新服通过之前，成果包继续保持`candidate`。
