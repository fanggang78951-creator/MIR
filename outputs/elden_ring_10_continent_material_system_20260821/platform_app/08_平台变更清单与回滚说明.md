# 平台变更清单与回滚说明

状态：**只生成仓库成果，未应用**
生成时间：2026-08-21T21:12:16+08:00

## 本轮实际变更

- 新增平台窗口成果JSON、XLSX、预检报告、冲突报告和契约验证脚本。
- 更新平台应用窗口`current`交班并新增不可覆盖的历史交班。
- 对`E:\XuanYuanDevPlatform`、`D:\MirServer`、`ApexM2.DB`、`MapInfo.txt`、`MonGen.txt`、`MonItems`的写入均为0。
- 未创建数据库备份、平台事务、安装收据或回滚包；因为没有发起目标写入。

## 后续安全应用顺序

1. 只把`01`中的65条新材料送入E盘平台材料预检；3条复用对象必须跳过新增，12条预留不得创建。
2. 预检确认数据库SHA、907—971仍空闲后，创建数据库/客户端目标文件的时间戳备份并记录事务号。
3. 只应用批次A，回读65条StdItems字段并核对3条复用对象未被改写。
4. 批次B必须等待怪物窗口提供32条真实怪物Name/身份与MonGen落点；`XX369`须先由地图权威资料解决。
5. 批次C必须等待装备窗口提供15件目标装备的Idx、来源编号和唯一模板；不得从当前空值推断。

## 回滚标准

- 当前提交的仓库成果可由Git逐文件恢复；不得用`git reset --hard`清除其它工作。
- 未来平台应用必须使用材料、怪物来源、装备三个独立事务，逐批保存写前哈希与收据。
- 任一目标文件或数据库被锁、预检后哈希改变、字段回读不一致时，整批停止并逐字节恢复该批写前备份。
- 回滚只覆盖该事务明确记录的目标，不能连带删除NPC、任务、称号或其它平台成果。
