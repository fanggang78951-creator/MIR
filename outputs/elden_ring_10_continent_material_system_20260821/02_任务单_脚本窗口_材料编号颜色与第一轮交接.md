# 脚本窗口任务单：材料编号、颜色、模板与第一轮交接

## 当前任务

你负责脚本系列。本轮先完成材料物品数据，不开始NPC、合成、称号或分解脚本施工。

工作分支：`codex/elden-ring-c01-v1-20260820`

先读取：

1. `outputs/elden_ring_10_continent_material_system_20260821/00_Codex材料系统_窗口分工.md`
2. `outputs/elden_ring_10_continent_material_system_20260821/01_艾尔登法环_十大陆材料合成NPC与编号总表.xlsx`
3. 工作簿的 `01_材料总表`、`07_ID登记`、`08_Codex回填任务`、`09_检查与替代`
4. 真实服务端物品库、既有材料模板与颜色字段；本地环境如存在，以 `D:\MirServer\Mud2\DB\ApexM2.DB` 的 `StdItems` 为准

## 具体执行

1. 扫描 `StdItems` 已用编号、废止编号和现有材料名称，先形成占用集合。
2. 对工作簿中的68个已命名材料逐项处理：
   - 已存在且名称、功能相同的，直接复用实际Idx。
   - 不存在的，分配真实未占用Idx。
   - 回填真实 `StdMode`、`Shape`、`Looks`、动画字段、颜色字段和值、显示颜色。
   - 统一设置为非绑定、可交易、NPC售价0；若引擎真实字段名不同，按真实字段填写并注明。
3. `失色锻造石`必须先查现有库，存在则复用，不得新建同名副本。
4. 保留12个预留材料位，只登记可用编号范围，不给它们临时起名。
5. 检查并列出：
   - `StdItems.Idx`自身重复；
   - 地图号自身重复；
   - 装备来源编号自身重复；
   - 地图号数字后缀与装备来源编号交叉重复。
6. 已确认的替代来源号保持为：821→6407、823→6401、824→6403；这里只核验，不改现有装备成品表。

## 本轮产物

在新目录中生成，不覆盖现有工作簿和已上传文件：

- `outputs/elden_ring_10_continent_material_system_20260821/phase1_script/材料编号颜色模板映射.xlsx`
- `outputs/elden_ring_10_continent_material_system_20260821/phase1_script/材料编号颜色模板映射.json`
- `outputs/elden_ring_10_continent_material_system_20260821/phase1_script/编号冲突与未解决项.md`
- `outputs/elden_ring_10_continent_material_system_20260821/phase1_script/交给怪物窗口的材料Idx映射.json`

JSON每项至少包含：`logicalMaterialId`、`name`、`continentId`、`actualIdx`、`table`、`templateIdx`、`stdMode`、`shape`、`looks`、`colorField`、`colorValue`、`tradeSettings`、`plannedSourceType`、`verificationState`。

## 交接条件

- 68个已命名材料全部有真实Idx或逐项写明无法分配的具体原因。
- 已填写的实际Idx重复数为0。
- 颜色和模板字段来自真实数据库，不以经验猜值。
- 提交以上4个新文件，并在提交说明中写：`phase1_script complete; monster window may continue`。
- 完成本轮后暂停，等待怪物窗口回传来源映射，再继续NPC、合成、称号与分解脚本。

