# Codex任务｜最终爆率与经济定稿归档和部署预检
仓库：fanggang78951-creator/MIR
工作分支：codex/elden-c1-c2-20260821

## 0. 授权
默认只归档＋只读预检。本包不是授权直接改服务器。用户另行明确部署后才写MonItems、回收脚本或数据库。
先读取本机D:\codex交班记录\00_任务总结索引.txt及本项目关联交班TXT；不扫描整盘。说明目标、读取、修改、风险、备份、验证。

## 1. 归档
1. 核验MANIFEST.json与SHA256SUMS.txt；三张XLSX、561份标准JSON、两套各561个TXT均在清单。
2. 先检查本地未提交改动。使用隔离干净工作区，git fetch，指定分支git pull --ff-only；分叉或其他任务变更冲突时停止集成，不reset --hard，不force push，不改main/master。
3. 复制ZIP的UPLOAD_ROOT内容到仓库对应相对路径。新增归档目录：codex_tasks/艾尔登法环_最终爆率与经济定稿_20260908/。不删除、重命名或覆盖此前任何归档。
4. 如果同路径已经存在且哈希一致，跳过；内容不同则另建revision目录并调整入口，不静默覆盖。
5. 根目录只允许更新“艾尔登法环_当前任务入口.md”。用本包替换稿与远端最新入口合并：旧信息保留必要继承；远端如有更新任务不得回退。
6. 仅stage新目录及根入口。git diff --cached --name-status应只有本批新增和根入口修改，无历史删除、数据库、游戏脚本或客户端文件修改。
7. 提交建议：design: finalize monster drop rates and self-farming economy
8. 普通push到指定分支。回读远端新commit/tree，比较三张XLSX、5351触发结果JSON及561怪索引的实际Git blob SHA256，不只看大小。
9. 回报commit SHA、分支、新增/修改/删除文件数、3工作簿哈希、561 UTF8/561 GB18030/561 JSON数量、git status，并记录本机交班。

## 2. 优先级
- 本包三张工作簿/标准JSON替代上一批“04_掉落内容与回收_爆率空置_V1.0.xlsx”中的空概率及旧小时估算。
- 817装备V3.0、561怪战斗属性、命格V2.2、既有配方单价、619失色拆解、20套制式价格及81材料回收价不因本包变更。
- 742条材料单次叠加数变动和25种补回的既有称号卷来源在逐怪表/改动表明示。
- 没有“先由用户审批数值”的待办；剩下是解析、编码、真实物品和字段的工程预检。

## 3. 必须做的只读工程预检
A. 使用“561怪文件索引.json”按准确怪名对接现有MonItems。逻辑怪物ID是索引，不要拿来新建重复怪物。
B. 单行1/N物品与#CHILD 1/N RANDOM沿用本项目旧形式。**1303条叠加行末尾的数量参数未在本机解析器验证**，不能假定第三列就是叠加数。读取本机已有成功案例/平台导出器；确认数量字段和物品堆叠规则，再以标准JSON转换适配TXT。不改变q数值，不拆为q个独立1/N判定。
C. UTF8与GB18030二选一，跟现服编码一致；不得两个目录一并加载。名称含“·”“[称号]”等必须逐字匹配实际库。未匹配的25称号和六神器只报告，不自动创建/改名/覆盖用户自定义属性。
D. 概率是基础1/N。现服只应用一次人物爆率；不要把有效概率再乘一次。确认最大爆率/人物爆率作用范围和饱和处理。文档模型p=min(1,M/N)，引擎整数除法有差异时集中换算，并保存原值。
E. 鞭尸使用现有系统，模型只取期望1+w；禁止再复制整个爆率表或重复加入鞭尸补偿。
F. 六神器一次触发六选一。兼掉普通/制式各为一次父池判定，不能将每个候选的1/1变成顶层独立必掉。
G. 装备产生后留用与拆解分开。回收增加50%只作用金币元宝；失色不乘1.5，不直接掉落，也不允许回收兑换。
H. 表内刷新数量与击杀/小时是假设，不是修改Mongen指令。读取现服实际供给，与模型12/600s、2/600s、1/1800s各类每种的假设对照，不能为凑小时值擅自覆盖刷新表。
I. 原每日额外守关礼包、活动、其他送物脚本仍独立，不合并成常规收益、不覆盖原脚本。
J. 大数金币余额、数量、分母、物品Idx及文本长度预检不允许溢出、截断；灵符原价保留，0=沿用的接口不写成免费。

## 4. 获得部署授权后
完整备份原MonItems和回收配置，保存逐文件SHA256与回滚清单。先隔离测试普通/稀有/地图BOSS/守关各代表文件，测试叠加材、随机池、鞭尸和物品名，再批量应用。仅写明确变更对象。
短样本只能验证解析和常见掉落；极稀有神器不得靠“刷几十只没出”判定错误，使用测试服临时概率或固定随机源验证后恢复正式分母。
写后回读实际文件，记录已归档/预检/已部署/进服测试四种状态。不要用模型核验替代引擎验收。
