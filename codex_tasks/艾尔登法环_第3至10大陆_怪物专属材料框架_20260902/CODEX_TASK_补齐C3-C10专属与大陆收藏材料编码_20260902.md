# Codex施工任务：补齐C3—C10专属装备与大陆收藏材料编码

任务日期：2026-09-02  
项目：传奇《艾尔登法环》  
GitHub仓库：`fanggang78951-creator/MIR`  
工作分支：`codex/elden-c1-c2-20260821`  
数据基线提交：`0f77a9c2d5d97ccca54a2983e172f9def3ef3009`（实际施工时必须先拉取远端最新提交，不得回退到此提交）

## 一、任务目标

补齐当前编码阶段留下的130个物理资源缺口，并形成可复核、可继续导入的正式交接成果：

1. 第3—10大陆专属装备来源码122个：
   - 武器60个。
   - 男衣服51个，严格使用平台确认的男衣服物理槽位，现行参考为`StdMode 10、位置0`。
   - 斗笠／面巾11个，严格使用`StdMode 16、位置13`。
2. 第3—10大陆收藏材料Items Looks 8个，对应已经规划的材料Idx 972—979。

任务完成后，464件专属应全部拥有唯一、同部位、可追溯的装备来源编号；8件大陆收藏材料应获得8个经过Items实库验证的空闲Looks。

## 二、固定目录与权威输入

开发平台现行唯一目录：

`E:\XuanYuanDevPlatform`

旧目录`D:\XuanYuanDevPlatform`已经失效，禁止使用。

服务端目录：

`D:\MirServer`

GitHub施工目录：

`codex_tasks/艾尔登法环_第3至10大陆_怪物专属材料框架_20260902`

必须读取以下当前文件，不得使用聊天记忆重建：

- `第3至10大陆_怪物专属与材料来源总账_V1.1_编码阶段分配.xlsx`
- `装备素材套装代码表.xlsx`
- `材料编码数据库.xlsx`
- `编码分配验证.json`
- `20260902_第3至10大陆专属与材料编码阶段分配_确认归档.md`
- `20260902_专属与材料编码阶段分配_完整成果包.zip`只作整包校验，不得优先于目录内单文件。

GitHub分支必须包含数据基线提交`0f77a9c2d5d97ccca54a2983e172f9def3ef3009`。若远端已有更新，以远端最新提交为基础继续，禁止`reset --hard`或强制推送。

## 三、绝对禁止事项

1. 不分配、不修改任何怪物模型或怪物编码。
2. 不修改地图、怪物名称、专属名称、装备属性、怪物属性、爆率、NPC、货币、配方和任务脚本。
3. 不改第一、第二大陆现有装备表和219个现用来源编号。
4. 不改已经锁定的342个第3—10大陆专属来源码，除非发现有实库冲突；发现冲突必须停下并提交证据，禁止自行换号掩盖。
5. 禁止用项链、戒指、生肖或其他部位的富余码代替武器、男衣服、斗笠／面巾缺口。
6. 女衣服`StdMode 11`不得自动并入通用衣服池；只有平台实测明确证明当前导入模板允许时，才可另列替代方案，不得直接写入正式结果。
7. 材料Looks不得使用6398—6462；这65个Items槽已被批次A材料占用。
8. 材料Looks不得机械顺延使用6463—6470；该段仍属于装备资源候选。
9. 禁止把材料图标写入DnItems或StateItem；材料只使用Items图库。
10. 禁止直接覆盖正式数据库、正式WZL/WZX或用户已有修改而不先备份。
11. 禁止把测试结果写成正式生产库已部署；禁止复用批次A的事务号、哈希或回读记录。
12. 禁止把数据库备份、账号凭据、Git令牌、完整生产WZL/WZX或其他敏感大文件提交到GitHub。
13. 不修改六份项目核心档案；本任务完成后由总规划窗口根据交接成果统一更新。

## 四、开始前检查

先输出一次简短施工前报告，然后在没有阻断项时继续执行，不必等待普通确认。报告必须包括：

- 当前GitHub分支与远端HEAD。
- 实际读取到的上述文件清单和SHA256。
- Git工作区是否存在用户未提交修改。
- 平台实际目录、测试目录、正式资源目录和目标数据库。
- 准备备份的确切文件与备份目录。
- 本次只写测试副本还是需要写正式资源；默认先写测试副本。

若Git工作区不干净，不得覆盖或提交用户无关修改。应使用独立worktree或新的干净克隆目录；禁止清理、丢弃或重置用户文件。

## 五、备份要求

在任何资源写入前，建立带时间戳的独立备份，并生成写前SHA256清单。至少覆盖：

- 本任务三张工作簿和验证JSON。
- 平台将要修改的项目配置与资源映射文件。
- 实际涉及的Items、StateItem、DnItems WZL/WZX文件。
- 若测试需要写数据库，备份对应测试数据库；涉及`D:\MirServer\Mud2\DB\ApexM2.DB`前必须明确它是测试副本还是正式库。

默认只允许在平台工作副本／测试资源包和测试数据库施工。正式资源或生产数据库只有在测试通过、写前再次备份且用户已明确授权时才能写入。

## 六、装备来源码施工

1. 从V1.1总账`09_编码缺口`读取122条待补记录，逐行保持记录ID、怪物、专属名称和部位关系不变。
2. 先读取平台当前部位定义、现有三图库和正式装备数据库占用，再决定新码；不得把数字空洞推定为空闲资源。
3. 每个装备来源编号必须：
   - 全服唯一。
   - 与第1、2大陆现用219码无交集。
   - 与第3—10大陆已锁定342码无交集。
   - 与材料Items Looks 6398—6462无交集。
   - 与平台现行装备及其他项目实际占用无交集。
   - 正确对应物理部位。
4. 按当前资源库规则，来源编号应能在Items、StateItem、DnItems三库中对应同一码且资源可读；若平台实际规则不同，必须以平台定义和实测为准，并在交接报告列出差异，不得猜填。
5. 新增资源必须逐项验证背包图标、地面图标、装备显示和部位，不允许只在Excel中写一个未落资源的数字。
6. 武器、男衣服、斗笠／面巾分别独立验收，严禁跨槽补齐。

## 七、大陆收藏材料施工

材料与Idx固定映射如下，不得调换：

| 大陆 | 材料名 | 逻辑ID | 固定Idx |
|---|---|---|---:|
| 宁姆格福西域 | 西境风痕碑拓 | EL-MAT-C03-COLLECT | 972 |
| 利耶尼亚海域 | 月潮忆珀 | EL-MAT-C04-COLLECT | 973 |
| 湖之利耶尼亚 | 湖心月谱残章 | EL-MAT-C05-COLLECT | 974 |
| 盖利德 | 熔原罪火残章 | EL-MAT-C06-COLLECT | 975 |
| 化圣雪原 | 永霜朝圣痕 | EL-MAT-C07-COLLECT | 976 |
| 巨人山顶 | 穹霜碑珀 | EL-MAT-C08-COLLECT | 977 |
| 亚坛高原 | 古坛鎏叶髓 | EL-MAT-C09-COLLECT | 978 |
| 王城罗德尔 | 穹律鎏痕拓片 | EL-MAT-C10-COLLECT | 979 |

施工要求：

1. 为8件材料提供8个经过Items.wzl/wzx实库确认的空闲Looks，不得与装备、材料或其他现行道具重叠。
2. 如需新建图标，制作8张彼此可辨认、与材料名称相符的48×48 RGBA源图，并保留源PNG与SHA256。
3. 图标只导入Items；导入后必须逐项回读，并核对源PNG与回读PNG。
4. 规划表中的`StdMode 46、Shape 1`只是沿用批次A材料基型的候选值；必须由平台模板或测试库确认后才能转为正式字段。
5. `Idx 980—983`继续冻结，不得用于本任务。
6. 默认只在测试数据库验证972—979与新Looks的映射；没有生产写入授权时，材料主索引仍应区分“测试通过／待正式入库”和“已正式入库”。

## 八、需要更新和新增的成果

在GitHub施工目录内提交：

1. `第3至10大陆_怪物专属与材料来源总账_V1.2_编码补齐.xlsx`
   - `怪物／模型编码`列仍统一写“用户自行分配／本表不处理”。
   - 464件专属来源码全部填齐。
   - 编码缺口变为0。
   - 保留逐行来源码、物理部位和资源状态。
2. 更新后的`装备素材套装代码表.xlsx`
   - 新增122个资源必须进入`全资源索引`。
   - 记录资源组、部位、StdMode、位置、Looks、三图库代码、来源状态和占用对象。
   - 结构化表格范围必须覆盖全部新增行。
3. 更新后的`材料编码数据库.xlsx`
   - 批次B规划表填入8个实际Looks及平台验证字段。
   - 未正式写生产库时不得把主索引扩写成“已部署”。
4. 更新后的`编码分配验证.json`。
5. 新建`20260902_C3-C10补码施工与验收报告.md`。
6. 新建`20260902_C3-C10新增资源占用清单.json`。
7. 如生成材料图标，提交8张源PNG和一份SHA256清单。

不得把备份目录、生产数据库、WZL/WZX整包或无关平台代码提交到仓库。

## 九、强制验收指标

交付前必须实际运行检查并在报告中列出结果：

- 概念专属464件。
- 已分配来源码464个。
- 待补来源码0个。
- 464码内部重复0。
- 与第一、第二大陆现用219码冲突0。
- 与材料Items Looks冲突0。
- 部位映射错误0。
- 武器新增60个、男衣服新增51个、斗笠／面巾新增11个，数量完全一致。
- 怪物模型／编码新增0个。
- 材料Idx 972—979共8个且唯一。
- 材料Items Looks共8个且唯一，空白0、与现有占用冲突0。
- 980—983占用0。
- 三张工作簿公式错误0。
- WZL/WZX或测试库写前、写后SHA256齐全。
- 逐项回读通过；任何未进服项目必须明确写“未验收”，不得概括为全部通过。

## 十、GitHub同步方法

### 1. 开始施工前

在仓库目录执行：

```powershell
git status --short
git fetch origin
git switch codex/elden-c1-c2-20260821
git pull --ff-only origin codex/elden-c1-c2-20260821
git rev-parse HEAD
git merge-base --is-ancestor 0f77a9c2d5d97ccca54a2983e172f9def3ef3009 HEAD
```

要求：

- `git pull --ff-only`失败时停止，不得强制合并或强推。
- 最后一条命令必须返回成功，证明数据基线仍在当前分支历史中。
- 若原工作区有无关修改，建立独立worktree或重新克隆；不得执行`git reset --hard`、`git clean -fd`或覆盖用户文件。

### 2. 完成验收后

只暂存本任务文件，先检查再提交：

```powershell
git status --short
git diff --check
git add -- "codex_tasks/艾尔登法环_第3至10大陆_怪物专属材料框架_20260902"
git diff --cached --name-only
git diff --cached --check
git commit -m "Complete C3-C10 equipment and material resource codes"
git push origin HEAD:codex/elden-c1-c2-20260821
```

禁止使用`git push --force`或`--force-with-lease`。

### 3. 推送后回读

```powershell
git fetch origin
git rev-parse HEAD
git rev-parse origin/codex/elden-c1-c2-20260821
git diff --quiet HEAD origin/codex/elden-c1-c2-20260821
git show --stat --oneline HEAD
```

本地HEAD与远端分支HEAD必须完全一致。随后逐个核对GitHub远端文件清单和本地SHA256；不能只看到`git push`成功就声称已经同步。

## 十一、停止条件

出现以下任一情况必须停止写入并报告，不得自行绕过：

- 无法证明候选码在对应物理图库和数据库中空闲。
- 新资源不能正确显示为指定部位。
- 发现现有342码、第1／2大陆219码或材料Looks存在冲突。
- 平台模板与现行`StdMode／位置`定义不一致。
- 需要修改怪物编码、装备属性、爆率或核心档案才能继续。
- 只能通过覆盖用户未提交修改、强制推送或直接写生产库才能完成。
- GitHub认证失败、分支发生非快进冲突或推送后远端哈希不一致。

## 十二、最终回报格式

完成后只需向用户交付：

1. 新增装备码数量与三个部位分项。
2. 8件材料的`Idx—Looks`逐项映射。
3. 测试资源、测试数据库、正式资源、生产数据库各自是否写入。
4. 所有验收数字、未通过项目和进服待验项目。
5. 备份目录及写前／写后SHA256清单位置。
6. GitHub提交SHA、提交链接、远端分支HEAD和变更文件清单。

不要只回复“已完成”。任何一项没有实际证据，就明确写“未完成／待验证”。
