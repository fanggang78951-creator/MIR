# Codex任务｜爆率前设计定稿归档与预检

仓库`fanggang78951-creator/MIR`，工作分支`codex/elden-c1-c2-20260821`。

## 本次授权范围
默认仅归档与只读预检。**不因收到本包就自动修改游戏服务器、数据库、资源或爆率文件。** 用户另行授权部署后才执行服务端写入步骤。

## 归档

1. 先读本机`D:\codex交班记录\00_任务总结索引.txt`及本项目关联交班TXT。修改前列出目标、读取、修改、风险、备份、验证。
2. 读取本包SHA256SUMS.txt/MANIFEST.json，核对每一个相对路径的实际字节。只复制`UPLOAD_ROOT`下的仓库相对路径。
3. 检查工作区有无未提交改动；不得吞掉其他任务改动。安全工作区切到指定分支，`git fetch`、`git pull --ff-only`。有分叉停止集成，禁止reset --hard或强推。
4. 新增`codex_tasks/艾尔登法环_爆率前设计定稿_20260908/`。不删除、不覆盖20260907基础或20260908既有增量目录。
5. 唯一常规可替换的旧文件为根`艾尔登法环_当前任务入口.md`。先比较远端最新入口：若只有此前审核版，使用本包入口；若已包含更新任务，保留新信息并追加本批，不能时间倒退。
6. 如果同名定稿目录已经存在且哈希完全一致，不重复提交；如果内容不同，生成新revision目录并调整入口，不默默覆盖。
7. 仅stage本批目录与根入口，检查diff没有旧目录删除/重命名、无数据库或其他业务脚本变化。
8. 建议提交：`design: finalize pre-drop gear progression and world balance`。普通push到指定工作分支，不改main/master。
9. 回读远端分支提交/树，至少校验5张业务XLSX和design_final.json的Git blob实际内容SHA256，不只比较文件大小。
10. 回报提交SHA、分支、新增/修改/删除文件数、远端哈希核对、git status；写入本机交班TXT及索引。

## 只读预检及之后的部署边界

- 逐一将817物品名称映射到独立物品Idx；同外观来源码可重复，物品Idx绝不可共用。优先按`变更清单`2773字段做补丁，不无差别全列覆盖。
- 按既有逻辑ID/精确名称定位561怪；未知Appr/HIT/SPEED保留旧值。攻击间隔毫秒通过平台既有转换，不直接写到SPEED。
- 171地图键按现服配置映射。C1的11个层名解析实际地图号；不猜号、不新建地图、不改变传送链路。所有同图住民统一韧性/处决值。
- 预检HP最大4.1×10^14的全链路支持范围，包括解析器、数据库、引擎、显示；禁止截断或环绕。受限时报告具体字段/上限/受影响行，不能偷偷修小。
- 对照619图鉴条目与46组、10大陆集齐计数，避免新增56件已经掉落却未计入图鉴或奖励重复。
- 命格绑定用当前平台字典；同名同时装备拒绝；幸运最高8绑定人物；C1圣律/圣物不改；C2升级最高280/件。
- 配方继续使用地图BOSS来源，C2是长期系统中心；对“0=沿用”的价格字段不能写成免费。
- 5400条掉落关系只用于最后的爆率设计，不把空概率写成0/1，不部署为空MonItems文件。六神器仍用现有池；未知本机追梦/称号卷不覆盖。
- 所有写入必须有备份、可回滚补丁、写后回读。分开记录“已归档”“预检通过”“已部署”“进服测试”，不相互冒充。
