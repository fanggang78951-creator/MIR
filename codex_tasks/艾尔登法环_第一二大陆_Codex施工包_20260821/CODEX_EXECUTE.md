# 《艾尔登法环》第一、第二大陆数值施工｜Codex执行单

## 执行目标

将本目录的四张平台格式修改表导入测试环境，完成第一大陆（漂流群岛）与第二大陆（宁姆格福）的怪物、制式装备和专属装备数值替换；资源/NPC总账只用于核算和后续补表，不直接导入平台。

## 应交给哪个Codex窗口

交给**能够直接访问并修改以下本机目录的“游戏数据／服务端施工Codex窗口”**：

- `D:\MirServer`
- `E:\XuanYuanDevPlatform`
- `D:\MirServer\AI_Handoff\任务文件`
- 当前服务端数据库、怪物库与装备库

不要交给纯聊天设计窗口，也不要交给只负责平台UI/应用代码、无法访问服务端数据的Codex窗口。

> 路径修正：开发平台已由`D:\XuanYuanDevPlatform`迁移至`E:\XuanYuanDevPlatform`。旧D盘平台路径已失效，不得继续作为执行目标；`D:\MirServer`及其任务目录不变。

## 文件与用途

1. `01_第一大陆36怪_属性修改表.xlsx`：12列正式怪物模板，36只。
2. `02_第二大陆宁姆格福61怪_属性修改表.xlsx`：13列正式怪物模板，61只。
3. `03_第一二大陆20套制式装备_属性修改表.xlsx`：66列正式装备模板，160件。
4. `04_第一二大陆105件专属装备_属性修改表.xlsx`：67列正式装备模板，105件。
5. `05_第一二大陆_数值依据_资源NPC总账.xlsx`：公式、资源字典、NPC矩阵、掉落/回收边界与质检，不作平台导入表。

## 绝对执行边界

- 不改地图、地图号、地图文件、传送坐标、刷新坐标、怪物名称、模型编号、装备名称、装备来源编号和剧情备注。
- 第一、第二大陆已经做好的魂魄材料、魂魄任务和任务奖励全部沿用。
- 地图号是`XX+数字`；装备来源编号是WZL纯数字排号。两套编号分开查重，不做跨类型同号告警。
- 所有武器必须有准确和攻速；武器先天幸运必须为空/0；武器和项链禁止防御、魔御；装备本体不写暴击几率。
- 材料正式模板、物品代码、NPC坐标、配方数量和合成成功率没有可靠来源时不得填写。
- 方案总账与平台导入表必须分开；不得把总账中的“待确认”写入生产库。

## 追梦神器特别规则

六件追梦神器已经由用户改成**六个时装首饰盒装备，可以同时佩戴**。本目录专属表的来源底板仍可能显示历史部位，因此：

1. 以用户本机最新装备库中的六个时装首饰盒部位为最高依据。
2. 按装备名称唯一匹配六件追梦神器。
3. 只覆盖数值字段，禁止覆盖`部位`、`类型`、`来源编号`和用户本机已修改的槽位信息。
4. 若任一名称不能唯一匹配，立即停止六件追梦神器的写入并列出对象；其余装备可继续按独立事务处理。

## 执行顺序

1. 将整个目录复制到`D:\MirServer\AI_Handoff\任务文件\EL_C1_C2_20260821`。
2. 读取本文件和`05_第一二大陆_数值依据_资源NPC总账.xlsx`的“交付说明”“总质检”。
3. 对目标装备表、怪物表、平台数据库及相关配置创建带时间戳的完整备份；输出备份清单和恢复命令。
4. 读取平台当前导出模板并核对工作表名、列数、表头、列顺序和字段类型：
   - 第一大陆怪物：12列，36行数据；
   - 宁姆格福怪物：13列，61行数据；
   - 制式装备：66列，160行数据；
   - 专属装备：67列，105行数据。
5. 如模板不一致，不做字段猜测或自动迁移；停止对应表并报告具体差异。
6. 在测试库中依次导入第一大陆怪物、宁姆格福怪物、制式装备、专属装备。
7. 专属表前99件可按完整行导入；最后6件追梦神器按名称只覆盖数值字段。
8. 执行唯一性与禁配检查：名称唯一、来源编号内部无重复、35件武器/时装武器准确和攻速非空、武器幸运为空、武器/项链防御魔御为空、暴击几率为空。
9. 启动测试服，至少完成以下实测：
   - 第一大陆普通怪、稀有、地图BOSS、守关BOSS各取样；确认整体低难度，普通怪与稀有怪攻击不会形成卡点。
   - 宁姆格福五个地图组各取普通、稀有、BOSS样本；记录标准角色实际DPS、单怪击杀时间、四怪围攻承伤、回血和吸血结果。
   - 葛瑞克记录完整战斗时长；六件追梦神器分别单戴和六件同戴，核对叠加结果。
10. 实测DPS/TTK相对总账计算基准偏差不超过15%才能继续；超过15%时停止生产导入，提交实测表，不凭经验改数。
11. 测试通过后再执行生产导入，并输出改动文件、数据库表、行数、前后差异、备份位置、回滚步骤和进服截图/日志。

## 预期验收结果

- 第一大陆36怪、第二大陆61怪全部唯一匹配。
- 制式160件、专属105件全部唯一匹配。
- 219个非空来源编号内部重复0；46个应留空来源编号继续为空。
- 第一大陆怪物最大攻击不高于888，HP保持原“正常血量”口径。
- 宁姆格福怪物HP由阶段DPS×目标击杀时间生成；最终守关BOSS方案HP为117,000,000、攻击18,900—28,888，必须由测试服实测决定是否正式落库。
- 六件追梦神器同时佩戴时，不恢复旧版每件神力30、爆伤50造成的六倍叠加。

## 完成回报格式

请在执行窗口回报：

1. 备份路径；
2. 实际导入文件与行数；
3. 未执行对象及原因；
4. 模板差异；
5. 来源编号/名称/禁配属性检查结果；
6. 五组宁姆格福DPS、TTK、承伤实测；
7. 追梦六件同戴叠加结果；
8. 是否已进入生产库；
9. 回滚命令。
